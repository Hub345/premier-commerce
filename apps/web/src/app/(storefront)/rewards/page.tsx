import { ComingSoon } from "@/components/site/ComingSoon";

export default function RewardsPage() {
  return (
    <ComingSoon
      kicker="Membership"
      title="Rewards"
      copy="Earning points on every purchase is on its way. We'll let you know the moment it lands."
    />
  );
}
